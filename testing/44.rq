# CQ-44: Which series does a parent data point reference point to?
# This involves traversing the Reference Pattern to a Data Point, and then identifying the Series that contains that Data Point (based on the Core Module structure).

PREFIX aml: <http://www.w3.org/animl/ontology/>

SELECT ?series
WHERE {
    ?reference a aml:AnimlReference ;
               aml:pointsTo ?dataPoint .
               
    ?dataPoint a aml:DataPoint .
    
    # Find the series containing this data point
    ?series a aml:Series ;
            aml:hasMember ?dataPoint .
}