# CQ-23: What series are defined in a series set blueprint for a technique?
# Based on the Technique Module, the technique contains specifications. A Series Set Blueprint aggregates Series Blueprints.

PREFIX aml: <http://www.w3.org/animl/ontology/>

SELECT ?seriesBlueprint
WHERE {
    ?technique aml:includes ?seriesSetBlueprint .
    
    ?seriesSetBlueprint a aml:SeriesSetSpecification ;
                        aml:hasPart ?seriesBlueprint .
                        
    ?seriesBlueprint a aml:SeriesSpecification .
}