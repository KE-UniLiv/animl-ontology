# CQ-87: What is the start value for an auto-incremented series?


PREFIX aml: <http://www.w3id.org/animl/ontology/>

SELECT ?startValue
WHERE {
    ?autoIncremented a aml:autoIncrementedType ;
                             aml:startValue ?startValue .
}