# CQ-46: Which item in the document was affected by an audit trail change?

PREFIX aml: <http://www.w3.org/animl/ontology/>

SELECT ?affectedItem
WHERE {
    ?change a aml:Change ;
            aml:affects ?affectedItem .
            
    ?affectedItem a aml:SignableItem .
}