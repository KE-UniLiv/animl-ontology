# CQ-6: What is the unique identifier of the device?
# requries populated validation

PREFIX aml: <http://www.w3.org/animl/ontology/>

SELECT ?device ?deviceID
WHERE {
    ?device a aml:Agent ;
    ?device aml:hasType aml:HardwareAgent ;
    ?device aml:identifier ?deviceID .
    
}
