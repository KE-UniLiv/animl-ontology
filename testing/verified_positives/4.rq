# CQ-4: What is the unique ID of an experiment step?
# requires population confirmation
PREFIX aml: <http://www.w3id.org/animl/ontology/>

SELECT ?stepID
WHERE {
    ?step a aml:ExperimentStep ;
          aml:id ?stepID .
}