# CQ-48: What was the reason given for a change in the audit trail?

PREFIX aml: <http://www.w3.org/animl/ontology/>

SELECT ?reason
WHERE {
    ?entry a aml:AuditTrailEntry ;
           aml:hasChange ?change .
           
    ?change aml:reason ?reason .
}